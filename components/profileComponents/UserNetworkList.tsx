import { View } from "react-native";

export default function UserNetworkLists({ id }: { id: string }) {
  const baseurl = process.env.EXPO_PUBLIC_BASE_URL || "http://192.168.252.240:3000";

  return (
    <View>

    </View>
  );
}